import Hero from "../components/hero";

export default function Homepage() {
  return (
    <section className="bg-neutral-900 h-screen flex items-center justify-center">
      <Hero />
    </section>
  );
}
